from scenedetect import VideoManager, SceneManager
from scenedetect.detectors import ContentDetector

"""
Using the Pydetect:
It is easier than the traditional method of utilising lines of code
for example:

def shot_boundaries(frames):

    
"""
